/**
 * Central locale config. Swap default to change app-wide number / date
 * formatting at once. Components should import APP_LOCALE rather than
 * hardcoding strings like "id-ID" or "en-US".
 */
export const APP_LOCALE = "en-US";
